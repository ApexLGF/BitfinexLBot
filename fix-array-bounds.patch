--- a/internal/strategy/smart_strategy.go
+++ b/internal/strategy/smart_strategy.go
@@ -33,7 +33,10 @@ func (ss *SmartStrategy) CalculateSmartOffers(fundsAvailable float64, fundingBo
 
 	// 添加市場數據到分析器
 	if len(fundingBook) > 0 {
-		currentRate := fundingBook[0].Rate
+		if len(fundingBook) == 0 {
+			return loanOffers // 安全返回空列表
+		}
+		currentRate := fundingBook[0].Rate
 		totalVolume := ss.calculateTotalVolume(fundingBook)
 		ss.analyzer.AddRateSnapshot(currentRate, totalVolume)
 	}
@@ -154,7 +157,10 @@ func (ss *SmartStrategy) calculateSmartHighHoldOffers(fundsAvailable *float64,
 	baseRate := ss.config.HighHoldRate
 	
 	// 市場利率參考
-	marketRate := fundingBook[0].Rate
+	if len(fundingBook) == 0 {
+		return offers
+	}
+	marketRate := fundingBook[0].Rate
 	
 	// 根據市場狀況調整利率
 	dynamicRate := ss.calculateDynamicHighHoldRate(baseRate, marketRate, condition)
@@ -337,8 +343,12 @@ func (ss *SmartStrategy) calculateProgressiveRate(fundingBook []*bitfinex.Fundi
 		rates[i] = book.Rate
 	}
 	
-	minRate := rates[0]
-	maxRate := rates[0]
+	if len(rates) == 0 {
+		return 0.001 // 默认利率
+	}
+	
+	minRate := rates[0]
+	maxRate := rates[0]
 	for _, rate := range rates {
 		if rate < minRate {
 			minRate = rate

--- a/internal/strategy/market_analyzer.go
+++ b/internal/strategy/market_analyzer.go
@@ -171,6 +171,10 @@ func (ma *MarketAnalyzer) calculateDynamicTargetRate(fundingBook []*bitfinex.Fu
 		return 0.001 // 默認目標利率
 	}
 	
+	if len(fundingBook) == 0 {
+		return 0.001 // 默認利率
+	}
+	
 	return fundingBook[0].Rate + avgSpread*0.3
 }

--- a/internal/strategy/lending.go
+++ b/internal/strategy/lending.go
@@ -571,6 +571,10 @@ func (lb *LendingBot) calculateKlineOffers(splitFundsAvailable float64) []*Loan
 		return offers
 	}
 	
+	if len(candles) == 0 {
+		return offers
+	}
+	
 	highestRate := candles[0].High
 	
 	// 根據配置選擇平滑方法
@@ -601,6 +605,10 @@ func (lb *LendingBot) calculateEMA(candles []*bitfinex.FundingCandle, period in
 		return 0
 	}
 	
+	if len(candles) == 0 {
+		return 0
+	}
+	
 	ema := candles[0].High
 	multiplier := 2.0 / (float64(period) + 1)
 	