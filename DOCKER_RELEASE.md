# BitfinexBot Docker 镜像发布 v2.1.0-hotreload

## 🚀 新功能发布

我们很高兴地宣布 BitfinexBot v2.1.0-hotreload 版本发布，这是一个重大更新，带来了配置热重载功能！

## 📦 Docker 镜像信息

**镜像地址**: `apexlgf/bitfenix-bot:latest`
**版本标签**: `apexlgf/bitfenix-bot:v2.1.0-hotreload`
**镜像大小**: ~83MB
**架构支持**: linux/amd64, linux/arm64

## ✨ 主要新功能

### 🔄 配置热重载系统
- **无需重启容器**即可应用新配置
- 智能的配置更新流程：取消订单 → 更新配置 → 重新初始化 → 开始交易
- 完整的配置验证和错误处理

### 🎛️ Web界面配置管理
- 直观的Web界面，支持实时修改所有重要参数
- 表单验证和确认对话框，防止错误配置
- 支持以下配置类型：
  - 基本设置（执行间隔、订单限制、贷出金额范围）
  - 利率策略（最低利率、分散笔数、市场深度）
  - 高额持有策略（利率、金额、订单数）
  - 智能策略参数（波动阈值、利率倍数）
  - 测试模式开关

### 🔧 改进和优化
- 修复funding credits统计问题
- 优化数据库架构，支持新的命令类型
- 改进Web界面布局和用户体验
- 增强错误处理和日志记录

## 🚀 快速开始

### 1. 拉取最新镜像
```bash
docker pull apexlgf/bitfenix-bot:latest
```

### 2. 运行容器
```bash
# 使用 docker-compose（推荐）
docker compose up -d

# 或者直接运行
docker run -d \
  --name bitfinex-bot \
  -v ./config.yaml:/app/config.yaml \
  -p 8080:80 \
  apexlgf/bitfenix-bot:latest
```

### 3. 访问Web管理界面
- 打开浏览器访问: `http://localhost:8080`
- 在"配置管理"区域实时调整机器人参数
- 点击"应用配置"立即生效，无需重启！

## 📋 配置热重载使用说明

1. **打开Web界面**: 访问 `http://localhost:8080`
2. **配置管理**: 在页面找到"配置管理"区域
3. **修改参数**: 根据需要调整各项参数
4. **应用配置**: 点击"应用配置"按钮
5. **确认生效**: 系统会自动执行热重载流程

### 热重载流程说明
```
📝 取消所有活跃订单
    ↓
🔧 更新应用程序配置
    ↓  
🤖 重新初始化放贷机器人
    ↓
🚀 使用新配置执行放贷策略
```

## 🔒 安全注意事项

- API密钥相关配置不支持热重载（需要重启容器）
- 所有配置更改都会进行验证，防止无效配置
- 建议在测试模式下验证配置后再应用到生产环境

## 📈 版本历史

- **v2.1.0-hotreload** (2025-08-03): 添加配置热重载系统和Web配置管理
- **v2.0.0**: Docker化部署与日志优化
- **v1.x**: 基础放贷机器人功能

## 🐛 问题反馈

如果遇到任何问题，请在GitHub仓库提交Issue：
https://github.com/ApexLGF/BitfinexLBot/issues

---

**🎉 现在就体验全新的配置热重载功能吧！**
